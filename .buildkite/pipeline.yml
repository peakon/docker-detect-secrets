plugins:
  - &ecr_plugin
    ecr#v2.1.0:
      login: true

steps:
  - label: "Build Secret Scanner"
    key: build-secret-scanner
    command:
      - docker build -t peakon/docker-detect-secrets .
      - ecr-push -a
    plugins:
      - *ecr_plugin
